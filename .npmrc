save-exact=true
# 7 days in minutes
minimum-release-age=10080
ignore-scripts=true
# Block transitive deps that resolve to git/tarball URLs instead of the registry
block-exotic-subdeps=true
# Refuse to install a package whose trust level dropped vs. prior versions
trust-policy=no-downgrade
